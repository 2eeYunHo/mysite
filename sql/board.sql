-- board

desc board;
select *
    from user;
    
    delete from board
				where no= 49
				and user_no= 4;
                
	select no, name, role
		  from user
		 where email=email
		   and password=password;
           
           select no, name, email, gender
		  from user
		 where no=no; 
         delete 
			from user
            where no = 7;